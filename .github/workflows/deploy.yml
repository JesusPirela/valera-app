name: Deploy to Hostinger

on:
  push:
    branches:
      - main

concurrency:
  group: deploy
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install dependencies
        run: npm ci

      - name: Build web
        run: npx expo export --platform web --clear
        env:
          EXPO_PUBLIC_SUPABASE_URL: ${{ secrets.EXPO_PUBLIC_SUPABASE_URL }}
          EXPO_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.EXPO_PUBLIC_SUPABASE_ANON_KEY }}

      # WhatsApp/Facebook no ejecutan JavaScript: al compartir /ficha/VR-265 leían
      # el HTML genérico de la SPA y la vista previa salía con el logo de la app.
      # ficha.php inyecta las etiquetas og: con la foto real de la propiedad.
      - name: Preparar vista previa de fichas (OpenGraph)
        env:
          SB_URL: ${{ secrets.EXPO_PUBLIC_SUPABASE_URL }}
          SB_ANON: ${{ secrets.EXPO_PUBLIC_SUPABASE_ANON_KEY }}
        run: |
          cp web/ficha.php dist/ficha.php
          # Con `sed` no: los secrets traen un salto de línea al final y rompen
          # la expresión. Aquí se sustituye leyendo del entorno y limpiando.
          python3 - <<'PY'
          import os
          ruta = 'dist/ficha.php'
          with open(ruta, encoding='utf-8') as f:
              s = f.read()
          s = s.replace('__SUPABASE_URL__', os.environ['SB_URL'].strip())
          s = s.replace('__SUPABASE_ANON__', os.environ['SB_ANON'].strip())
          with open(ruta, 'w', encoding='utf-8') as f:
              f.write(s)
          PY

      - name: Create .htaccess
        run: |
          cat > dist/.htaccess << 'EOF'
          Options -Indexes -MultiViews

          RewriteEngine On

          # Bloquear basura heredada del deploy viejo por Git (node_modules, .git,
          # etc. quedaron públicos en la raíz web). Devuelven 403 en vez de servirse.
          RewriteRule ^(node_modules|dist|\.expo|\.git)(/|$) - [F,L]

          # Forzar HTTPS
          RewriteCond %{HTTPS} !=on
          RewriteRule ^ https://%{HTTP_HOST}%{REQUEST_URI} [R=301,L]

          # Las fichas compartidas pasan por ficha.php, que devuelve el MISMO
          # index.html pero con las etiquetas og: de esa propiedad (foto, título
          # y precio) para que la vista previa de WhatsApp muestre la casa.
          RewriteRule ^ficha/([A-Za-z0-9._-]+)/?$ /ficha.php?codigo=$1 [L,QSA]

          # SPA: las rutas como /ficha/VR-1184 no son archivos reales; las sirve
          # index.html y el router del cliente decide qué pintar.
          # Antes esto se hacía con `ErrorDocument 404 /index.html`, que devuelve
          # el HTML correcto pero CON ESTADO 404: los navegadores dentro de apps
          # (WhatsApp en iOS) muestran su propia pantalla de error ante un 404 y
          # la ficha compartida se veía en blanco. Con RewriteRule se sirve con 200.
          RewriteCond %{REQUEST_FILENAME} !-f
          RewriteCond %{REQUEST_FILENAME} !-d
          RewriteRule ^ index.html [L]

          # Red de seguridad por si mod_rewrite no estuviera activo.
          ErrorDocument 404 /index.html

          # Cabeceras de seguridad
          Header always set X-Content-Type-Options "nosniff"
          Header always set X-Frame-Options "SAMEORIGIN"
          Header always set Referrer-Policy "strict-origin-when-cross-origin"
          Header always set Permissions-Policy "camera=(), microphone=(), geolocation=()"
          Header always set Strict-Transport-Security "max-age=31536000; includeSubDomains"

          <FilesMatch "\.html$">
            Header set Cache-Control "no-cache, no-store, must-revalidate"
            Header set Pragma "no-cache"
            Header set Expires 0
          </FilesMatch>

          <FilesMatch "\.(js|css|png|jpg|ico|woff2)$">
            Header set Cache-Control "public, max-age=31536000, immutable"
          </FilesMatch>
          EOF

      # Deploy directo por FTP al servidor de Hostinger. Antes se subía a una rama
      # web-build y Hostinger la "jalaba" con un webhook, pero ese jalado se
      # atascó (dejó basura .git/node_modules en la raíz web y congeló el sitio):
      # los cambios llegaban a main pero NO a la web. El FTP sube dist/ directo y
      # es el método que funcionaba de forma confiable.
      # A veces el FTP de Hostinger rechaza la conexión ("Failed to connect") de
      # forma transitoria y tumbaba el deploy entero. Se intenta hasta 3 veces.
      - name: Deploy a Hostinger por FTP (intento 1)
        id: ftp1
        continue-on-error: true
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_HOST }}
          username: ${{ secrets.FTP_USER }}
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: ./dist/
          server-dir: ./
          # El estado de sincronización viejo (.ftp-deploy-sync-state.json) quedó
          # desincronizado: alguien subió un build viejo por FTP fuera de esta
          # Action, así que index.html apuntaba al bundle antiguo pero la Action
          # creía que ya estaba al día ("File content is the same, doing nothing")
          # y NO subía nada. Cada deploy salía "success" pero la web no cambiaba.
          # Con un state-name nuevo la Action re-sube TODO y corrige index.html.
          state-name: .deploy-state-v2.json

      - name: Esperar y reintentar (intento 2)
        if: steps.ftp1.outcome == 'failure'
        run: sleep 25
      - name: Deploy a Hostinger por FTP (intento 2)
        id: ftp2
        if: steps.ftp1.outcome == 'failure'
        continue-on-error: true
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_HOST }}
          username: ${{ secrets.FTP_USER }}
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: ./dist/
          server-dir: ./
          state-name: .deploy-state-v2.json

      - name: Esperar y reintentar (intento 3)
        if: steps.ftp1.outcome == 'failure' && steps.ftp2.outcome == 'failure'
        run: sleep 30
      - name: Deploy a Hostinger por FTP (intento 3, final)
        if: steps.ftp1.outcome == 'failure' && steps.ftp2.outcome == 'failure'
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_HOST }}
          username: ${{ secrets.FTP_USER }}
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: ./dist/
          server-dir: ./
          state-name: .deploy-state-v2.json
